apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: advweb-db-data
  namespace: advweb
  name: advweb-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
